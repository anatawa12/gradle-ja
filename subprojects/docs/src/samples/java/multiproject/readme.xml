<sample>
    <para><phrase role="original">This sample demonstrates how an application can be composed using multiple Java projects.</phrase>
        複数のJavaプロジェクトを使ってアプリケーションを組み立てるデモ</para>

    <para role="original">This build creates a client-server application which is distributed as 2 archives. First, there is a client
        ZIP which includes an API JAR, which a 3rd party application would compile against, and a client runtime. Then,
        there is a server WAR which provides a web service.
    </para>
    <para>
    このビルドはクライアント・サーバー型のアプリケーションを作成するもので、配布物になるのは二つのアーカイブです。
    サードパーティ製のアプリケーションをコンパイルするためのAPIを含むJARとクライアントのランタイムが入ったZIPファイル、
    そして、Webサービスを提供するサーバーWARファイルを作成します。
    </para>
</sample>
